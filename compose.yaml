services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: web-api
    ports:
      - "5000:5000"
    environment:
      NODE_ENV: production
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
